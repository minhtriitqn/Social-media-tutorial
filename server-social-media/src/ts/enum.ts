export enum EnumRole {
  ADMIN = 'admin',
  USER = 'user',
}

export enum EnumTypeMessage {
  STRING = 'string',
  IMAGE = 'image',
  VIDEO = 'video',
}

export enum EnumRelationShip {
  SINGLE = 0,
  DATING = 1,
  MARRIED = 2,
}
